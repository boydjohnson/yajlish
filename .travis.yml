language: rust
rust:
  - stable
  - beta
  - nightly
jobs:
  allow_failures:
    - rust: nightly
before_script:
  - rustup component add clippy
  - rustup component add rustfmt
script:
  - cargo build --verbose
  - cargo test --verbose
  - cargo fmt -- --check
  - cargo clippy
